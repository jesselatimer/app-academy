<h2>New Tweet!</h2>
<form class="tweet-compose" data-tweets-ul="#feed">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <strong class="chars-left">140 characters remaining</strong>
  <br>

  <a href="javascript:void(0)" class="add-mentioned-user">Add mention</a>
  <div class="mentioned-users">
  </div>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>


<script type="text/template" id="add-mention-script">
  <label>
    Mention
    <select name="tweet[mentioned_user_ids][]">
      <option></option>
      <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
      <% end %>
    </select>
  </label>
</script>
