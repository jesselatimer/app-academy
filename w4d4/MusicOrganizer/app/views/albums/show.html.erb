<h1>Album: <%= @album.title %></h1>

<p>Yup, this sure is <%= @album.title %>.
   I was recorded by <%= link_to @album.band.name, band_url(@album.band) %>.
   I'm a <%= @album.studio.downcase %> recording.</p>

<h3>Tracks</h3>
<ul>
 <% @album.tracks.each do |track| %>
   <li><%= link_to track.title, track_url(track) %></li>
 <% end %>
</ul>

<%= button_to "New Track", new_album_track_url(params[:id]), method: :get %>
<%= button_to "Edit Album", edit_album_url(@album), method: :get %>
<%= delete_album %>
