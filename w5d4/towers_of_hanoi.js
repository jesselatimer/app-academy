var stacks = [[1, 2, 3], [], []];
var win = false;

while (!win) {
  console.log(stacks);
  var fromStack = prompt("Which stack to move from?");
  var toStack = prompt("Which stack to move to?");
  
};
